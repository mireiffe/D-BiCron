{
  "_comment": "source/target 에 databases.json 의 DB ID(문자열) 또는 인라인 접속 정보(dict) 지정",
  "source": "source_db_id",
  "target": "target_db_id",
  "tables": [
    {
      "table": "device_master",
      "pk_columns": ["id"],
      "ts_column": "updated_at",
      "source_schema": "public",
      "target_schema": "public"
    },
    {
      "table": "sensor_readings",
      "pk_columns": ["device_id", "measured_at"],
      "ts_column": "created_at",
      "overlap_minutes": 60
    }
  ]
}
